import { Oxygen } from "next/font/google";

const oxygen300 = Oxygen({
  weight: "300",
  subsets: ["latin"],
  display: "swap",
});

const oxygen700 = Oxygen({
  weight: "700",
  subsets: ["latin"],
  display: "swap",
});

const oxygen400 = Oxygen({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export { oxygen300, oxygen400, oxygen700 };
